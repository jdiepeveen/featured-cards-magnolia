[#assign itemElement = def.parameters.itemElement!"div"]
[#assign itemClass = def.parameters.itemElement!"div"]

[#assign title = ""]
[#assign image = ""]
[#assign text = ""]

<${itemElement} class="${itemClass}">
	<img src="">
	<h3></h3>
	${text}
</${itemElement}